# Junction41 Dashboard — Environment Variables
# Copy to .env.local for development, .env.production for prod builds

# API base URL (REST endpoints)
VITE_API_URL=http://localhost:3000

# WebSocket URL (Socket.IO for chat, workspace, real-time events)
VITE_WS_URL=http://localhost:3000
